"use client"; import BrandLogo from "@/components/BrandLogo"; import {useState} from "react"; import Image from "next/image"; import Link from "next/link"; import {ArrowRight, Check, Sparkles} from "lucide-react"; import {useCreateUserWithEmailAndPassword} from "react-firebase-hooks/auth"; import {auth,firebaseConfigured} from "@/app/firebase/firebaseConfig"; import {useRouter} from "next/navigation";
export default function Signup(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[create,,loading,error]=useCreateUserWithEmailAndPassword(auth);const router=useRouter();const submit=async(e:React.FormEvent)=>{e.preventDefault();if(!firebaseConfigured)return;const res=await create(email,password);if(res)router.push("/dashboard/student")};return <main className="app-bg noise grid min-h-screen lg:grid-cols-2"><section className="flex flex-col px-6 py-6 sm:px-12 lg:px-16"><BrandLogo /><div className="mx-auto my-auto w-full max-w-md py-12"><p className="eyebrow">Start your journey</p><h1 className="mt-3 text-4xl font-semibold">Create your account</h1><p className="muted mt-3">A clearer path starts with a few simple details.</p><button className="secondary-btn mt-8 w-full">Continue with Google</button><div className="my-6 flex items-center gap-4 text-xs text-slate-600"><span className="h-px flex-1 bg-white/[.08]"/>OR CONTINUE WITH EMAIL<span className="h-px flex-1 bg-white/[.08]"/></div><form onSubmit={submit} className="space-y-5"><label className="block text-sm">Email<input className="field mt-2" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" required/></label><label className="block text-sm">Password<input className="field mt-2" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="At least 8 characters" minLength={8} required/></label><div className="flex gap-2 text-xs text-slate-500"><Check size={14} className="text-green-400"/> By continuing, you agree to Aspire&apos;s terms and privacy policy.</div><button disabled={loading||!firebaseConfigured} className="primary-btn w-full disabled:cursor-not-allowed disabled:opacity-50">{loading?"Creating account...":"Create account"}<ArrowRight size={16}/></button></form>{!firebaseConfigured&&<p className="mt-4 text-sm text-amber-300">Account creation is in preview mode until Firebase environment variables are added.</p>}{error&&<p className="mt-4 text-sm text-rose-400">{error.message}</p>}<p className="mt-7 text-center text-sm text-slate-400">Already have an account? <Link className="text-blue-400" href="/auth/login">Log in</Link></p></div></section><section className="relative hidden overflow-hidden border-l border-sky-300/[.1] bg-[#07112a]/55 backdrop-blur-xl lg:flex lg:items-center lg:justify-center"><div className="absolute h-[420px] w-[420px] rounded-full bg-blue-600/10 blur-3xl"/><div className="relative text-center"><Image src="/brand/aspire-space-mascot.png" width={390} height={560} alt="Aspire AI mentor" className="mx-auto h-[480px] w-auto object-contain"/><div className="surface-strong mx-auto -mt-8 max-w-sm p-5 text-left"><p className="text-sm font-semibold">“I&apos;ll help you turn uncertainty into a next step.”</p><p className="mt-2 text-xs text-slate-500">Aspire, your AI career mentor</p></div></div></section></main>}
