"use client"; import BrandLogo from "@/components/BrandLogo"; import {useState} from "react"; import Image from "next/image"; import Link from "next/link"; import {ArrowRight,Sparkles} from "lucide-react"; import {useSignInWithEmailAndPassword} from "react-firebase-hooks/auth"; import {auth,firebaseConfigured} from "@/app/firebase/firebaseConfig"; import {useRouter} from "next/navigation";
export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[login,,loading,error]=useSignInWithEmailAndPassword(auth);const router=useRouter();const submit=async(e:React.FormEvent)=>{e.preventDefault();if(!firebaseConfigured)return;const res=await login(email,password);if(res)router.push("/dashboard/student")};return <main className="app-bg noise flex min-h-screen"><div className="mx-auto flex w-full max-w-[1180px] items-center gap-16 px-6 py-10"><section className="w-full max-w-md"><div className="mb-16"><BrandLogo /></div><p className="eyebrow">Welcome back</p><h1 className="mt-3 text-4xl font-semibold">Continue your journey</h1><p className="muted mt-3">Your roadmap and mentor are ready when you are.</p><form onSubmit={submit} className="mt-8 space-y-5"><label className="block text-sm">Email<input className="field mt-2" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" required/></label><label className="block text-sm"><span className="flex justify-between">Password<a href="#" className="text-blue-400">Forgot password?</a></span><input className="field mt-2" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Your password" required/></label><button disabled={loading||!firebaseConfigured} className="primary-btn w-full disabled:opacity-50">{loading?"Signing in...":"Log in"}<ArrowRight size={16}/></button></form>{!firebaseConfigured&&<p className="mt-4 text-sm text-amber-300">Login is in preview mode until Firebase environment variables are added.</p>}{error&&<p className="mt-4 text-sm text-rose-400">{error.message}</p>}<p className="mt-7 text-center text-sm text-slate-400">New to Aspire? <Link className="text-blue-400" href="/auth/signup">Create an account</Link></p></section><section className="relative hidden flex-1 items-center justify-center lg:flex"><div className="absolute h-96 w-96 rounded-full bg-blue-600/10 blur-3xl"/><Image src="/brand/aspire-space-mascot.png" width={420} height={600} alt="Aspire AI mentor" className="relative h-[530px] w-auto object-contain"/></section></div></main>}
