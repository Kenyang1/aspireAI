'use client';
import {Suspense,useEffect,useState} from 'react'; import {useRouter,useSearchParams} from 'next/navigation'; import {Award,Check,Lightbulb,RefreshCw,Sparkles} from 'lucide-react'; import ReactLoading from 'react-loading'; import {questionData} from '../../../../../data/data';

function ResultsContent(){const[audioFiles,setAudioFiles]=useState(null);const[questions,setQuestions]=useState([]);const[roleBrief,setRoleBrief]=useState(null);const[loading,setLoading]=useState(true);const[feedback,setFeedback]=useState([]);const router=useRouter();const params=useSearchParams();
useEffect(()=>{try{const active=JSON.parse(sessionStorage.getItem('active_questions')||'[]');const indexes=(params.get('questions')||'').split(',').map(Number).filter(Number.isInteger);setQuestions(active.length?active:indexes.map(index=>questionData[index]).filter(Boolean));const prep=JSON.parse(sessionStorage.getItem('tailored_prep')||'null');setRoleBrief(prep?.roleBrief||null)}catch{setQuestions([])}try{setAudioFiles(JSON.parse(sessionStorage.getItem('audio_files')||'[]'))}catch{setAudioFiles([])}},[params]);
useEffect(()=>{if(!audioFiles||!questions.length){if(audioFiles)setLoading(false);return}const assess=async()=>{try{const results=[];for(const[index,url]of audioFiles.entries()){const response=await fetch(url);const blob=await response.blob();const transcription=await fetch('/api/transcription',{method:'POST',headers:{'Content-Type':'application/octet-stream'},body:new File([blob],`answer-${index}.wav`,{type:'audio/wav'})});if(!transcription.ok)throw new Error(transcription.statusText);const transcript=(await transcription.json()).transcription;if(!transcript){results.push(null);continue}const question=Array.isArray(questions[index])?questions[index][0]:questions[index];const assessment=await fetch('/api/agents/assess',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question,transcript,roleBrief})});results.push(assessment.ok?(await assessment.json()).feedback:null)}setFeedback(results)}catch(error){console.error('Error generating feedback:',error)}finally{setLoading(false)}};assess()},[audioFiles,questions,roleBrief]);
if(audioFiles===null)return <div className="grid min-h-[60vh] place-items-center"><ReactLoading type="bubbles" color="#60a5fa"/></div>;
return <main className="page-wrap"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">Interview feedback</p><h1 className="page-title mt-2">Your practice review</h1><p className="muted mt-2">Start with your strengths, then focus on one improvement at a time.</p></div><button onClick={()=>router.push('/dashboard/student/mockinterview')} className="secondary-btn"><RefreshCw size={16}/> Practice again</button></div>{loading?<div className="surface mt-8 grid min-h-[420px] place-items-center"><div className="text-center"><ReactLoading type="bubbles" color="#60a5fa" className="mx-auto"/><p className="muted mt-3 text-sm">Aspire is reviewing your answers...</p></div></div>:questions.length===0?<div className="surface mt-8 p-10 text-center"><p className="font-semibold">No completed interview was found.</p><button onClick={()=>router.push('/dashboard/student/mockinterview')} className="primary-btn mt-5">Start a practice</button></div>:<div className="mt-8 space-y-5">{questions.map((question,index)=>{const item=feedback[index];const label=Array.isArray(question)?question[0]:question;return <article key={index} className="surface overflow-hidden"><div className="border-b border-white/[.07] p-5 sm:p-6"><p className="text-xs text-slate-500">QUESTION {index+1}</p><h2 className="mt-2 text-lg font-semibold">{label}</h2>{audioFiles[index]&&<audio controls className="mt-4 w-full"><source src={audioFiles[index]} type="audio/wav"/></audio>}</div><div className="grid gap-5 p-5 sm:p-6 lg:grid-cols-[150px_1fr_1fr]"><div><p className="text-xs text-slate-500">STAR SCORE</p><p className="mt-2 text-4xl font-semibold text-blue-300">{item?.starScore??'-'}<span className="text-base text-slate-500">/5</span></p></div><div><p className="flex items-center gap-2 font-semibold text-green-400"><Check size={16}/> Strengths</p><ul className="mt-3 space-y-2 text-sm text-slate-400">{item?.strengths?.length?item.strengths.map((text,i)=><li key={i}>{text}</li>):<li>No feedback available.</li>}</ul></div><div><p className="flex items-center gap-2 font-semibold text-amber-300"><Lightbulb size={16}/> Focus next</p><ul className="mt-3 space-y-2 text-sm text-slate-400">{item?.gaps?.length?item.gaps.map((text,i)=><li key={i}>{text}</li>):<li>Keep practicing with specific examples.</li>}</ul></div></div>{item?.rewriteSuggestion&&<div className="mx-5 mb-5 rounded-lg border border-blue-400/15 bg-blue-500/[.06] p-4 text-sm text-slate-300 sm:mx-6 sm:mb-6"><span className="mr-2 inline-flex text-blue-400"><Sparkles size={15}/></span>Try this approach: {item.rewriteSuggestion}</div>}</article>})}<section className="surface-strong flex flex-col items-start gap-4 p-6 sm:flex-row sm:items-center"><Award className="text-amber-300"/><div className="flex-1"><p className="font-semibold">Every practice builds confidence</p><p className="muted mt-1 text-sm">Save one improvement as your next goal, then come back when you are ready.</p></div><button onClick={()=>router.push('/dashboard/student/mockinterview')} className="primary-btn">Practice again</button></section></div>}</main>}
export default function Results(){return <Suspense fallback={<div className="grid min-h-[60vh] place-items-center"><ReactLoading type="bubbles" color="#60a5fa"/></div>}><ResultsContent/></Suspense>}
